---
version: '2.2.0'
extends: 'template.yaml'

database:
    host: '%env(DB_HOST)%'
    port: '%env(DB_PORT)%'
    user: '%env(DB_USER)%'
    password: '%env(DB_PASSWORD)%'
    name: '%env(DB_NAME)%'

dump:
    output: 'php://stdout'

faker:
    locale: 'fr_FR'
