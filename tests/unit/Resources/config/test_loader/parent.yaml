extends:
    - 'parent_parent.yaml'
    - 'template'

if_version:
    '>=2.0.0':
        tables:
            table1:
                converters:
                    field2:
                        converter: 'anonymizeText'
