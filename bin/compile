#!/usr/bin/env php
<?php
use Smile\GdprDump\Compiler;

// Define the root directory of the application
define('APP_ROOT', dirname(__DIR__));

// Initialize autoload
require APP_ROOT . '/vendor/autoload.php';

// Run the compiler
$compiler = new Compiler();
$compiler->compile('gdpr-dump.phar');
