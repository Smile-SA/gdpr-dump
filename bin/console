#!/usr/bin/env php
<?php
use Smile\Anonymizer\AppKernel;

// Define the root directory of the application
define('APP_ROOT', dirname(__DIR__));

// Initialize autoload
require APP_ROOT . '/vendor/autoload.php';

// Run the console application
$application = new AppKernel();
$application->run();
