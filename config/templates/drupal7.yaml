---
requiresVersion: true

tables:
    cache:
        truncate: true

    cache_*:
        truncate: true

    sessions:
        truncate: true

    users:
        converters:
            mail: 'obfuscateEmail'
            init:
                converter: 'obfuscateEmail'
                unique: true
            name:
                converter: 'obfuscateText'
                unique: true
            pass: 'anonymizeText'
